# lib/ — dependency jars

Quarkus fast-jar bootstrap (`boot/`) and application classpath (`main/`).
Jars here are produced by `./build/package-dist.sh` and are gitignored.
Do not replace with an uber-jar.
